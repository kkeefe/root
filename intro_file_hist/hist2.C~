//create a histogram generating random integers to fill between 1 and 6


{
  h2 = new TH1I("h2", "Dice Values", 6, 1, 6);

  for(int i = 0; i < 1000; i++){
    h2->Fill(gRandom->Uniform(1, 6));
    i++;
  }
  
  h2->Draw();
}
